spring:
  application:
    name: service2 # Important to be the same as the file name in config server
  profiles:
    active:
      default # Important to be one of the config file options i have in file name on config server
  cloud:
    config:
      enabled: true
